#include "image.hpp"

using namespace rasterimage;

image::image(
    const r4::vector2<uint32_t>& dimensions,
    pixel_format pf,
    depth channel_depth
) :
    imvar(
        [&](){}(
            
        )
    )
{
    // TODO:
}
